import React from "react";
import HeroCarousel from "../Components/HeroCarousel";

const Home = () => {
  return (
    <div>
      <HeroCarousel></HeroCarousel>
    </div>
  );
};

export default Home;
